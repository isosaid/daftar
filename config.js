// Ключи Firebase — вставьте свои, чтобы работали вход и синхронизация.
// Где взять: console.firebase.google.com → ваш проект → Project settings →
// раздел "Your apps" → Web app → SDK setup and configuration → Config.
// Пока apiKey пустой, платформа работает без входа, только на этом устройстве.
window.FIREBASE_CONFIG = {
  apiKey: "",
  authDomain: "",
  projectId: "",
  appId: ""
};
